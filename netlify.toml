# Base configuration for Netlify deployment
[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# Redirects are handled by the Netlify Vite plugin or _redirects file.
# Do NOT add redirects for /.netlify/functions/* or /.netlify/images here,
# as Netlify handles these paths internally.